# Fonts

## Committed (SIL Open Font License)

| Path | Used by |
|------|---------|
| `alegreya-sans/` | Résumé body (`parts/preamble.tex`) |
| `Alegreya/` | Serif family (OFL; optional) |
| `books/short-reach-optics/fonts/` | Book body (`books/short-reach-optics/preamble.tex`) |

Each OFL directory includes `OFL.txt`.

## Local only (gitignored)

Proprietary Adobe fonts not used by the builds: `adobe-clean/`, `gill-sans-nova/`,
`minion-pro/`, `myriad-pro/`.

## Fontin Sans

`fontin-sans/` is committed for résumé section titles. Check the Fontin license
before redistributing outside this repo.
